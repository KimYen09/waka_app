<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow the debug app to access the local HTTP API. Release builds must
         use HTTPS instead. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
